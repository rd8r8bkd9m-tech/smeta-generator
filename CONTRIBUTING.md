# Руководство для контрибьюторов

Спасибо за интерес к проекту SMETA PRO! Мы рады любому вкладу в развитие проекта.

## 🚀 Начало работы

### Требования

- Node.js 20+
- pnpm 8+
- Rust 1.75+ (для WASM модулей)
- PostgreSQL 15+ (для локальной разработки)
- Docker (опционально)

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/your-org/smeta-pro.git
cd smeta-pro

# Установка зависимостей
pnpm install

# Настройка окружения
cp apps/api/.env.example apps/api/.env

# Запуск базы данных (через Docker)
docker-compose up -d postgres redis

# Применение миграций
pnpm --filter api db:migrate

# Заполнение тестовыми данными
pnpm --filter api db:seed

# Запуск в режиме разработки
pnpm dev
```

## 📝 Процесс внесения изменений

### 1. Создание Issue

Перед началом работы создайте Issue с описанием:
- Для багов: шаги воспроизведения, ожидаемое и фактическое поведение
- Для фич: описание функционала и его польза

### 2. Создание ветки

```bash
# Для фич
git checkout -b feature/название-фичи

# Для багфиксов
git checkout -b fix/описание-бага

# Для документации
git checkout -b docs/что-документируем
```

### 3. Разработка

- Следуйте стилю кода проекта
- Пишите тесты для нового функционала
- Документируйте публичные API

### 4. Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавить калькулятор накладных расходов
fix: исправить расчет НДС
docs: обновить README
test: добавить тесты для калькулятора
refactor: оптимизировать запрос к БД
```

### 5. Pull Request

1. Убедитесь, что все тесты проходят: `pnpm test`
2. Проверьте линтер: `pnpm lint`
3. Создайте PR с описанием изменений
4. Свяжите PR с Issue

## 🏗️ Структура проекта

```
smeta-pro/
├── apps/
│   ├── web/              # React PWA приложение
│   ├── api/              # Express API сервер
│   └── telegram-bot/     # Telegram бот
├── packages/
│   ├── wasm-calculator/  # Rust WASM расчетный модуль
│   ├── shared/           # Общие типы и константы
│   └── ui/               # UI компоненты
├── data/
│   ├── normatives/       # Нормативные базы
│   ├── templates/        # Шаблоны документов
│   └── prices/           # База цен материалов
└── docker/               # Docker конфигурация
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
pnpm test

# Тесты с покрытием
pnpm test -- --coverage

# Тесты конкретного пакета
pnpm --filter web test
pnpm --filter api test
```

## 📚 Стиль кода

### TypeScript

- Используем строгий режим TypeScript
- Предпочитаем функциональный подход
- Используем именованный экспорт

```typescript
// Хорошо
export function calculateTotal(items: Item[]): number {
  return items.reduce((sum, item) => sum + item.price * item.quantity, 0)
}

// Плохо
export default function(items) {
  let sum = 0
  for (let i = 0; i < items.length; i++) {
    sum += items[i].price * items[i].quantity
  }
  return sum
}
```

### React

- Функциональные компоненты
- Хуки для состояния и эффектов
- Мемоизация где необходимо

```tsx
// Хорошо
interface ButtonProps {
  onClick: () => void
  children: React.ReactNode
}

export function Button({ onClick, children }: ButtonProps) {
  return <button onClick={onClick}>{children}</button>
}
```

### CSS

- Используем Tailwind CSS
- Компонентный подход
- Минимум кастомных стилей

## 🔒 Безопасность

При обнаружении уязвимости:
1. **НЕ** создавайте публичный Issue
2. Напишите на security@smeta-pro.ru
3. Дождитесь ответа в течение 48 часов

## 📄 Лицензия

Внося изменения в проект, вы соглашаетесь с тем, что ваш код будет распространяться под лицензией MIT.

## 🤝 Сообщество

- [Discord](https://discord.gg/smeta-pro)
- [Telegram](https://t.me/smeta_pro)
- [GitHub Discussions](https://github.com/your-org/smeta-pro/discussions)

## ❓ Вопросы

Если у вас есть вопросы, создайте Discussion или напишите в чат сообщества.

---

Спасибо за ваш вклад! 🎉
